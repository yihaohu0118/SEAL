# SEAL Experiment

This folder contains the standalone BFCL V3 multi-turn Full SEAL training
configuration.

Example:

```bash
python -m agentevolver.main_ppo \
  --config-path exp \
  --config-name SEAL
```
